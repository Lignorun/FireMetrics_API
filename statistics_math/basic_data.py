# statistics_math.basic_data.py

from data.cache_manager import (
    get_raw_data_from_cache,
    set_raw_data_to_cache,
    show_all_data
)

def basic_stats(local_type: str, local_code: str, grouping: str, interval: str):
    return None